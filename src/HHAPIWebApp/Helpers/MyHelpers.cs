using Microsoft.AspNet.Mvc.Rendering;
using Microsoft.AspNet.Mvc.ViewFeatures;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace HHAPIWebApp.Helpers
{
    public static class MyHtmlHelpers
    {
        /*public static HtmlString PairValues(this HtmlHelper htmlHelper,
           Dictionary<String, object> dict)
        {
           //declare the html helper 
            var builderdl = new TagBuilder("dl");

            foreach(var e in dict)
            {
                var builder = new TagBuilder("li");
                builder = "";
            }
            //hook the properties and add any required logic
            builder.MergeAttribute("src", source);
            builder.MergeAttribute("alt", alternativeText);
            //create the helper with a self closing capability
            // Render tag
            var ret = new HtmlString(builder.ToString());
            return ret;
        }*/
    }
}
